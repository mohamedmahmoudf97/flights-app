import { Card, CardHeader, Container } from "@mui/material"

const FlightPage = () => {
    return <Container>
        <Card>
            <CardHeader title={'Flight page'} />
        </Card>
    </Container>
}
export default FlightPage